<p id="notice"><%= notice %></p>

<h1><%= MiningType.model_name.human(count: @mining_types.count) %></h1>

<table class='table table-hover'>
  <thead>
    <tr>
      <th><%= I18n.t("activerecord.attributes.mining_type.description") %></th>
      <th><%= @mining_types.human_attribute_name(:acronym) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mining_types.each do |mining_type| %>
    <tr>
      <td><%= mining_type.description %></td>
      <td><%= mining_type.acronym %></td>
      <td><%= link_to I18n.t('links.show'), mining_type %></td>
      <td><%= link_to t('links.edit'), edit_mining_type_path(mining_type) %></td>
      <td><%= link_to t('links.delete'), mining_type, method: :delete, data: { confirm: t('messages.confirm') } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- <nav class="nav">
  <li><%= link_to t('links.new'), new_mining_type_path, class: "nav-link" %></li>
  <li><%= link_to t('links.back'), welcome_index_path, class: "nav-link" %></li>
</nav> -->
<%= link_to t('new.mining_type'), new_mining_type_path, class: "btn btn-primary" %>
<%= link_to t('links.back'), welcome_index_path, class: "btn btn-primary" %>